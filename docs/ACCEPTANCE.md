# 首次设备验收

**当前状态：未执行。自动检查结果不代表设备验收通过。**

顺序：本地检查 → Mac 候选 → iPhone → 审阅并首次公开发布 → 开启每日自动发布。AI 故障测试不通过时停在候选阶段，不降低住宅出口要求。

## A. 操作前

1. 两端记录 Shadowrocket 版本、系统版本、配置模式、精确节点名称、当前配置和更高优先级模块。记录仅放 `private/acceptance/`，不要附私密原始日志到仓库。
2. 将当前设备配置导出备份到本地 `private/`，确认可重新选择；不删除或改名原始节点。住宅节点应“代理通过”精确的普通 V3。
3. 确认当前软件能解析三个单成员 select 分组和全部配置设置。按名称绑定，不能用包含字符串的模糊筛选。
4. 本次导入、激活候选配置、重连 VPN 都会改变 Mac 网络路由，操作前取得本次明确确认。关闭旧 AI 会话与旧连接后再测试，温和策略可能保留既有 TCP 连接。

## B. 先用无账号探测检查出口与失败行为

先建独立测试副本，复用同样的策略结构，只把公开 IP 回显探测目标分别加入 OpenAI / Claude 组。不用登录态，也不把真实 AI 账号当探针。记录成功请求实际出口，核对住宅出口；仅看策略名称或延迟值不算通过。

| 场景 | 预期与记录要求 |
|---|---|
| 正常节点链 | OpenAI、Claude 两组均显示住宅最终出口，中转链正确 |
| 更换首页默认节点 | 两组继续走住宅；新连接结果不随首页改变 |
| 住宅节点不存在 | 用测试副本指向不存在的测试别名，不删除原始节点；请求失败 |
| 住宅节点不可达 | 仅对测试副本设置不可用的本地测试端口；请求失败，不出现普通 V3 / DIRECT 出口 |
| 中转节点不存在 | 在隔离的测试住宅副本中设置不存在的中转别名；请求失败 |
| 中转不可达 | 只改隔离测试副本，中转不可连接；请求失败 |
| IPv4 | 明确使用 IPv4 目标，正常与上述故障均得到预期 |
| IPv6 | 在实际可用的 IPv6 环境中测试正常和故障；环境不可用须标“未测”，不能填通过 |
| UDP / QUIC | 使用可观察的测试接收端，验证代理侧 QUIC 被阻断、不支持 UDP 时 REJECT，故障不直连。curl 的 TCP 成功不能代替 UDP 验收 |

如果缺失住宅节点导致软件自动选首页节点或 DIRECT，单成员 select 的方案就没有满足要求：立即停止候选启用，记录实际行为，返回实现阶段解决。`close-if-proxy-chain-missing` 只保护中转缺失，不能作为住宅异常通过的证据。

无账号故障探测通过后，先在住宅出口保护下收集真实 AI 服务请求，检查新域名是否落在住宅规则内。不要先让未知端点用登录态访问后再检查出口。独立的 GitHub、包仓库或用户指定外部站点按目标地址正常分流。

## C. 真实功能与分流

| 项目 | 验收动作 | 当前结果 |
|---|---|---|
| OpenAI / ChatGPT / Codex | 登录、对话、图片 / 文件上传、常用代码流程；逐项核对目标规则和实际出口 | 未测 |
| Claude | 登录、对话、图片 / 文件上传、常用流程；共享依赖允许归 OpenAI 组但出口仍须住宅 | 未测 |
| Apple 备忘录 | Mac 新建文字 → iPhone；iPhone 修改 → Mac；双向添加图片 / 文件附件，分别复测 Wi-Fi 与蜂窝；检查 CloudKit 与 APNs | 未测 |
| 同花顺 | 行情、新闻、登录等代表性请求直连，12 条保留 | 未测 |
| 哔哩哔哩 | 首页、图片、视频、账号请求正常且按规则直连 | 未测 |
| 其他国内重点应用 | 微信、知乎、抖音等首批代表性请求；其余 personal.json 名单分批补测 | 未测 |
| 字节例外 | `bytedapm.com` 命中常规境外；`snssdk.com` 命中 DIRECT | 未测 |
| 懂球帝 | 冷启动与常用页面广告效果；资讯、图片、比赛数据正常；残留广告单独记录域名，不能承诺全部去除 | 未测 |
| Google | 声明仍在；未启用 HTTPS 解密时不把 HTTPS 跳转效果当作已通过 | 未测 |

Apple / 国内的直连 DNS 失败代理兜底是已接受设置；正常解析路径应直连，解析失败时的代理请求单独记录原因。Apple Intelligence 和 ChatGPT 语音不在第一版功能验收范围。

## D. iPhone 与配置更新

iPhone 由用户按上述步骤操作并反馈。两端共同核对同一规则版本。

首次公开发布前，可在仅本机可访问的临时测试地址验证 Mac 的下载异常恢复；iPhone 可使用用户已有的私密传输方式导入同一候选，不创建临时公开站点。稳定 GitHub 地址的下载验收只能在该仓库实际发布后完成，因此把“候选文件的加载与错误恢复”和“发布后的本人地址复核”分开记录。首次发布前设备加载 / 故障保护仍必须先通过；公开后先复核地址，再启用自动发布。

对短文件、错误页面、无法下载分别试验更新行为，旧的已验收配置必须能恢复。不要拿原始打底当作已经满足住宅约束的恢复配置。设置后台更新间隔 1 天，并测试手动更新、核对首行 `rules-version`。系统限制可能延迟后台运行。

## 脱敏结果格式

每项只写设备、软件版本、时间、规则版本、用例、通过 / 失败 / 未测、实际策略与“出口是否符合住宅”的结论。节点地址、完整 IP、账号、token、连接原文留在 `private/`。报告不得把“尚未发现漏项”写成“所有未来端点保证覆盖”。

在这里记录真正执行的验收摘要后，才可据实填写 `policy/release.json`。当前没有任何设备项通过。
