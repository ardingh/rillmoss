# 首次仓库交付记录

2026-09-10 用户明确确认后，已在 work 账号创建公开仓库 [ardingh/rillmoss](https://github.com/ardingh/rillmoss)，将 76 个已审阅文件普通推送到 `main`。首次提交为 `b2171475b8bf0c5f9f4b73c0e70b9375d0290962`，远端文件清单与本地审阅清单一致。没有切换全局 GitHub 账号。

本次交付是**公开的开发候选仓库**：代码、完整候选、来源快照和已知问题一并公开；GitHub 每日及手动检查来源、记录结果并保存候选，但不自动替换成品。用户已暂缓进一步设备故障实验；本次公开不代表日常版本验收通过。

## 首次公开内容

| 内容 | 状态 |
|---|---|
| `rillmoss.conf` | 42,367 条内嵌规则，Mac / iPhone 共用的开发候选 |
| `rillmoss/`、`tests/` | 标准库生成、严格解析、完整快照、变更检查与 Git 写回；34 项测试通过 |
| `policy/` | 个人选择、35 个选定来源、成功条数基线、发布暂停设置 |
| `snapshot/`、`version.json` | 当前完整成功来源和可重建版本记录 |
| `.github/workflows/` | 每日台北时间 08:08、手动更新、串行任务；仅仓库 token 写回允许文件 |
| `checks/`、`reports/` | 真实检查结果、脱敏设备结果；保留已知失败，未测不标通过 |
| README、维护/来源/验收说明 | 使用方式、个人决定、来源范围、更新和恢复；候选状态明确可见 |

节点地址和凭据、设备备份、截图、原始连接日志、试验副本、本地状态文件均留在忽略目录或忽略文件，不属于公开清单。首次公开前的核对包括已配置的两个节点地址/凭据值与常见密钥格式；不在报告中列出这些值。

## 本轮主线验证

- 自动测试：34 项通过；完整快照重建验证通过。
- 真实在线更新检查：2026-09-10 20:06:58 台北时间，全部 35 项来源通过。本轮规则无变化，原成品、版本和构建时间未被伪装为更新。
- 每日更新只在全部来源成功时采用完整候选；异常增删、下载失败、未知格式、并发任务及远端变化均有阻断。
- 两份工作流 YAML 语法检查通过。生成的配置、快照和版本文件发生普通 push 变更时，也会触发验证工作流。
- 时区语法按 [GitHub 官方说明](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#onschedule) 核对。已实跑手动工作流；每日计划为台北 08:08，尚未观察到首次定时事件，实际触发可能延迟。
- [首次 GitHub 来源检查](https://github.com/ardingh/rillmoss/actions/runs/34475893320)成功：35 项来源全部通过，只写回两个检查文件，当前成品与版本不变；完整候选 Artifact 已下载并通过离线重建校验。
- 首次代码工作流暴露同秒记录导致并发测试未触发推送的测试问题。修正后本地 34 项通过，[云端复核](https://github.com/ardingh/rillmoss/actions/runs/34476489456)也通过；未修改分流或放宽失败检查。
- 稳定配置地址已通过 HTTPS 下载，与审阅候选逐字节一致。work 账号已有站内及默认邮箱的失败工作流通知设置；未改设置，未验证邮件实际送达。

本地工程验证见 `reports/engineering-review-20260910.json`；云端运行、公开地址和候选校验记录见 `reports/github-publication-20260910.json`。

## 当前候选限制

正常连接初测中，ChatGPT / Claude 回显出口符合住宅节点，11 项重点规则命中正确。但住宅节点名称不存在时，原候选中的两个 AI 分组实际回退到普通 V3；此缺陷尚未修复。后续 REJECT 组合试验在解析预检查时停止，未发出新的出口探测。

本次公开已保留候选标识、失败证据和 `publish_enabled: false`。每日任务只抓取、检查并记录结果；不把试验副本或新候选自动替换为成品。GitHub 首次公开与将来启用日常版本、恢复成品自动发布分开确认。

## 已执行的授权范围

1. 使用 work 账号创建一个公开仓库 `ardingh/rillmoss`。
2. 普通推送已审阅本地项目至该仓库 `main`，不强推，不创建 GitHub Release。
3. 仓库中的每日与手动工作流可运行来源检查、写回真实检查记录并保存候选 Artifact；成品发布开关保持关闭。

公开后文件可被他人复制，之后改私有或移除内容不能保证撤回他人副本。以上动作已在用户对本次范围明确确认后执行；没有开启成品自动发布，没有创建 GitHub Release。
