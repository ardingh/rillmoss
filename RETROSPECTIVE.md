# rillmoss 验证复盘

## 2026-09-10：单成员分组不等于节点缺失时拒绝

- 事实：Mac Shadowrocket 2.2.90 (3378) 中，单成员 select 指定不存在节点后，OpenAI 和 Claude 的无账号测试目标都经普通 V3 成功到达服务器。配置参数、中转缺失保护和规则匹配均被识别，但实际出口违反要求。
- 原因边界：已证明当前分组写法在该客户端上存在节点缺失回退；未取得客户端源代码，不推断所有内部选择逻辑。`close-if-proxy-chain-missing` 的启用不能作为最终住宅节点缺失已受保护的证据。
- 防止再犯：正常出口检查与失败时出口检查分别记录。既检查原生策略日志，也比较实际回显。已失败的候选禁止启用和发布；试验性的 REJECT 保护只有缺失失败、正常住宅成功两项均完成后才可考虑采用。
- 证据：`reports/mac-missing-residential-20260910.json`；原始日志和回显仅保留本机忽略目录。
- 范围与归位：仅本项目设备验收。现有 `AGENTS.md` 和 `docs/ACCEPTANCE.md` 已要求静态检查不能替代故障验收，本次沿用这些边界，不新增全局规则、Skill 或跨任务经验条目。

后续试验观察：REJECT 与精确正则并列的写法在该客户端中解析为空 `items`，脚本在无账号探测前正确停止。此前只借其他已导入组核对参数存储形状，没有证明组合写法会保留固定 REJECT；因此不能凭模拟数据测试通过就安排完整 VPN 重连。以后若恢复这类实验，先只导入并检查未启用配置，解析正确后再由用户安排网络切换。没有放宽空组检查，已加实际空列表作为回归样本。当前按用户要求暂缓实验，回到工程交付，不新增全局规则。
