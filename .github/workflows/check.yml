name: Validate code and stored configuration
on:
  push:
    paths:
      - 'rillmoss/**'
      - 'policy/**'
      - 'tests/**'
      - '.github/workflows/**'
      - 'rillmoss.conf'
      - 'snapshot/**'
      - 'version.json'
      - 'reports/build.json'
  pull_request:
permissions:
  contents: read
jobs:
  tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.14'
      - run: python -m unittest discover -s tests -v
      - run: python -m rillmoss verify --bundle .
